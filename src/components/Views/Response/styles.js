import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  root: {
    alignItems: 'center',
    padding: 5
  },
  image: {
    height: 200,
    width: 200,
    borderRadius: 15,
  },
  text:{
      fontWeight: "bold",
      fontSize: 15
  }
});

export default styles;